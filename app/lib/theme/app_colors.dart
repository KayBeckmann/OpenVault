import 'package:flutter/material.dart';

abstract final class AppColors {
  // Surface tiers
  static const background = Color(0xFF0B1326);
  static const surfaceContainerLowest = Color(0xFF060E20);
  static const surfaceContainerLow = Color(0xFF131B2E);
  static const surfaceContainer = Color(0xFF171F33);
  static const surfaceContainerHigh = Color(0xFF222A3D);
  static const surfaceContainerHighest = Color(0xFF2D3449);
  static const surfaceBright = Color(0xFF31394D);

  // On-surface
  static const onSurface = Color(0xFFDAE2FD);
  static const onSurfaceVariant = Color(0xFFCBC3D7);

  // Primary (Obsidian Purple)
  static const primary = Color(0xFFD0BCFF);
  static const onPrimary = Color(0xFF3C0091);
  static const primaryContainer = Color(0xFFA078FF);
  static const onPrimaryContainer = Color(0xFF340080);
  static const inversePrimary = Color(0xFF6D3BD7);

  // Secondary
  static const secondary = Color(0xFFC0C1FF);
  static const onSecondary = Color(0xFF1000A9);
  static const secondaryContainer = Color(0xFF3131C0);
  static const onSecondaryContainer = Color(0xFFB0B2FF);

  // Tertiary
  static const tertiary = Color(0xFFFFB869);
  static const onTertiary = Color(0xFF482900);
  static const tertiaryContainer = Color(0xFFCA801E);

  // Error
  static const error = Color(0xFFFFB4AB);
  static const onError = Color(0xFF690005);
  static const errorContainer = Color(0xFF93000A);

  // Outline
  static const outline = Color(0xFF958EA0);
  static const outlineVariant = Color(0xFF494454);

  // Inverse
  static const inverseSurface = Color(0xFFDAE2FD);
  static const inverseOnSurface = Color(0xFF283044);
}
